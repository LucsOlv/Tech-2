services:
  osrm:
    image: osrm/osrm-backend
    container_name: osrm_server
    ports:
      - "5000:5000"
    volumes:
      - ./osrm-data:/data
    command: osrm-routed --algorithm mld /data/rio-de-janeiro-latest.osrm
    restart: always